FOLLOW(SStart) = { < EOF > }

FOLLOW(Start) = { < EOF > }

FOLLOW(Tokens) = { < SKIP >, < ID > }

FOLLOW(Token) = { < } >, < , >}

FOLLOW(Skip) = { < ID > }

FOLLOW(Rules) = { < EOF > }

FOLLOW(Rule) = { < ) >, < ; > }

FOLLOW(Closure) = { < ) >, < ; > }

FOLLOW(BasicRule) = { < ) >, < ; > }

FOLLOW(RuleElement) = { < ) >, < ; >  }

FOLLOW(RuleBlock) = { < { >, < ) >, < ; > }

FOLLOW(Annotation) = { < ) >, < ; >}

FOLLOW(AnnotationOption) = { < } > }